#include <stdio.h>
#include <stdlib.h>

enum game_const{
    width   = 25;
    length   = 80;
};

ina main() {
    // создаем массив поля
    // создаем буффер чтобы запонмить предыдущий шаг
    // считываем массив поля
    // выводи массив поля
    // читаем скрость
    // двигаем клеточки

    return 0;
}
